package Lab6;
import java.util.*;

public class Lab6Calculator {

    public static void main(String[] args) {
        Scanner input = new Scanner( System.in );
        String userInput = input.next();

        LinkedList<Double> user = new LinkedList<>();

        if(userInput.equals( '+') || userInput.equals( '-' ) || userInput.equals( '*' ) ||
        userInput.equals( '/' )){
        // TODO: pop and calculate then push back to stack
        } else {
            // TODO: convert number to a double value and push to stack
        }

        // TODO: Pop and print remainging number fron stack
    }

}
